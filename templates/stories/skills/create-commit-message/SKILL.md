# create-commit-message

Reference skill for generating commit messages from card context. Phase
`<phase>.commit-message` scripts print the message to stdout; Devflow captures
it and does not stream it to the console (req §13.4).
