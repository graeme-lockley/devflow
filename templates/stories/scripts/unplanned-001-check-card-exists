#!/usr/bin/env bash
set -euo pipefail
card_dir="${DEVFLOW_CARD_DIR:?DEVFLOW_CARD_DIR not set}"
if [ ! -f "${card_dir}/state.json" ]; then
  echo "state.json missing" >&2
  exit 1
fi
if [ ! -f "${card_dir}/card.md" ]; then
  echo "card.md missing" >&2
  exit 1
fi
exit 0
